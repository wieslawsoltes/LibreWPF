<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ProGPU.Wpf.SdkSwitchLibrary">
    <SolidColorBrush x:Key="LibraryThemeFillBrush" Color="#D9E6F2" />
    <SolidColorBrush
        x:Key="{ComponentResourceKey TypeInTargetAssembly={x:Type local:LibraryThemedControl}, ResourceId=LibraryThemeBorderBrush}"
        Color="#B25C3D" />

    <Style TargetType="{x:Type local:LibraryThemedControl}">
        <Setter Property="Foreground" Value="#223344" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:LibraryThemedControl}">
                    <Border
                        x:Name="LibraryThemeRoot"
                        Background="{StaticResource LibraryThemeFillBrush}"
                        BorderBrush="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type local:LibraryThemedControl}, ResourceId=LibraryThemeBorderBrush}}"
                        BorderThickness="2"
                        Padding="{TemplateBinding Padding}">
                        <TextBlock
                            x:Name="LibraryThemeText"
                            Foreground="{TemplateBinding Foreground}"
                            Text="{TemplateBinding Text}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
