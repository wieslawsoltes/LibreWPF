<Window
    x:Class="ProGPU.Wpf.HelloApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="RootWindow"
    Title="ProGPU WPF Hello"
    Width="520"
    Height="360"
    MinWidth="420"
    MinHeight="280">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="Padding" Value="12,7" />
        </Style>
    </Window.Resources>

    <Grid
        Margin="20"
        Background="{DynamicResource HelloSurfaceBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock
                x:Name="TitleText"
                FontSize="26"
                FontWeight="SemiBold"
                Text="ProGPU WPF Hello" />
            <TextBlock
                x:Name="SubtitleText"
                Margin="0,4,0,0"
                Foreground="{DynamicResource HelloAccentBrush}"
                Text="{Binding Status}" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Margin="0,18,0,0"
            Orientation="Horizontal">
            <TextBox
                x:Name="NameBox"
                Width="230"
                VerticalContentAlignment="Center"
                Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                x:Name="UpdateButton"
                Margin="10,0,0,0"
                Click="OnUpdateButtonClick"
                Content="Update" />
        </StackPanel>

        <ListBox
            x:Name="ItemsList"
            Grid.Row="2"
            Margin="0,18,0,0"
            ItemsSource="{Binding Items}" />

        <TextBlock
            x:Name="FooterText"
            Grid.Row="3"
            Margin="0,16,0,0"
            Text="{Binding Footer}" />
    </Grid>
</Window>
